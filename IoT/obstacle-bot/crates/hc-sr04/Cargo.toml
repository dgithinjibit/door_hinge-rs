[package]
name = "hc-sr04"
version = "0.1.0"
description = "Platform-agnostic embedded-hal 1.0 driver for the HC-SR04 ultrasonic distance sensor. Works on AVR / Cortex-M / ESP32 / RP2040."
keywords = ["hc-sr04", "ultrasonic", "embedded-hal", "driver", "no-std"]
categories = ["embedded", "hardware-support", "no-std"]
readme = "README.md"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
embedded-hal = { workspace = true }
