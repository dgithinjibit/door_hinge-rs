# Conservative defaults. The defaults are good; we pin only what the team has
# agreed on so future edits don't churn the diff.
edition = "2021"
max_width = 100
use_field_init_shorthand = true
use_try_shorthand = true
