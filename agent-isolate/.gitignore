# Rust build artifacts
/target
**/*.rs.bk

# We commit Cargo.lock — this workspace produces a binary, and reproducible
# builds matter for a security tool. (Cargo's own guidance: lock binaries.)

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# Local config / scratch
*.local.yaml
*.local.yml
/scratch/
/tmp/

# Profiling / perf
flamegraph.svg
perf.data*
